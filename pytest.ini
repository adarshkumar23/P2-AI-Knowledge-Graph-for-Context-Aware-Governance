[pytest]
testpaths = tests core-side-patch/tests
# eu_india_biometric_case.py is intentionally not prefixed "test_" (its name
# is dictated by PATENT.md's "Required Evidence Before Filing" section /
# CLAUDE_CODE_GOAL_PROMPT.md Workstream E), so it is added explicitly here
# alongside the default test_*.py pattern used by every other test module.
python_files = test_*.py eu_india_biometric_case.py
